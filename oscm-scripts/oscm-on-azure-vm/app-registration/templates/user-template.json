{
  "displayName": "${displayName}",
  "mailNickname": "${userId}",
  "accountEnabled": true,
  "userPrincipalName": "${userId}@${tenantName}",
  "passwordProfile" : {
    "forceChangePasswordNextSignIn": false,
    "password": "${password}"
  }
}
